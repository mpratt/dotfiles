# Virtual Hosts
#
# Required modules: mod_log_config

# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.4/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#
<VirtualHost *:80>
   DocumentRoot "/mnt/data" 
   ServerName testlocalhost.loc
   ServerAlias www.testlocalhost.loc
   <Directory "/mnt/data">
        Options All
        AllowOverride All
        Order deny,allow
        Allow from 127.0.0.1
        Require ip 127.0.0.1 
   </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@michael-pratt.loc
    DocumentRoot "/mnt/data/Proyectos/Web/MichaelPratt"
    ServerName michael-pratt.loc
    ServerAlias www.michael-pratt.loc
    ErrorLog "/var/log/httpd/michael-pratt_error_log"
    CustomLog "/var/log/httpd/michael-pratt_access_log" common
    <Directory "/mnt/data/Proyectos/Web/MichaelPratt">
        Options FollowSymlinks
        AllowOverride All
        Order deny,allow
        Allow from 127.0.0.1
	Require ip 127.0.0.1
    </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@vayausted.loc
    DocumentRoot "/mnt/data/Proyectos/Web/VayaUsted"
    ServerName vayausted.loc
    ServerAlias www.vayausted.loc
    ErrorLog "/var/log/httpd/vayausted_error_log"
    CustomLog "/var/log/httpd/vayausted_access_log" common
    <Directory "/mnt/data/Proyectos/Web/VayaUsted">
        Options All
        AllowOverride All
        Order deny,allow
        Allow from 127.0.0.1
        Require ip 127.0.0.1
    </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@phpmyadmin
    DocumentRoot "/var/www/htdocs/adminer"
    ServerName phpmyadmin.loc
    ServerAlias www.phpmyadmin.loc
    ServerAlias adminer.loc
    ServerAlias www.adminer.loc
    ErrorLog "/var/log/httpd/phpmyadmin_error_log"
    CustomLog "/var/log/httpd/phpmyadmin_access_log" common
    <Directory "/var/www/htdocs/adminer">
        Options All
        AllowOverride All
        Order deny,allow
        Allow from 127.0.0.1
        Require ip 127.0.0.1
    </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@lab
    DocumentRoot "/mnt/data/Proyectos"
    ServerName lab.loc
    ServerAlias www.lab.loc
    ErrorLog "/var/log/httpd/lab_error_log"
    CustomLog "/var/log/httpd/lab_access_log" common
    <Directory "/mnt/data/Proyectos">
        Options All
        AllowOverride All
	Order deny,allow
        Allow from 127.0.0.1
        Require ip 127.0.0.1
    </Directory>
</VirtualHost>
